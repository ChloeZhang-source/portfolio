---
import { getCollection } from 'astro:content';
import About from '../components/About.astro';
import Contact from '../components/Contact.astro';
import Footer from '../components/Footer.astro';
import Header from '../components/Header.astro';
import Hero from '../components/Hero.astro';
import ChapterRule from '../components/ChapterRule.astro';
import SideAtmosphere from '../components/SideAtmosphere.astro';
import WorkCard from '../components/WorkCard.astro';
import BaseLayout from '../layouts/BaseLayout.astro';
import { homeCopy } from '../data/home';

const works = (await getCollection('works')).sort((a, b) => a.data.order - b.data.order);
---

<BaseLayout>
	<Header slot="header" />
	<SideAtmosphere />
	<Hero works={works.map((entry) => entry.data)} />
	<section id="work" class="chapter">
		<div class="chapter__art" aria-hidden="true">
			<ChapterRule />
		</div>
		<p class="chapter__kicker">作品</p>
		<p class="chapter__thesis">{homeCopy.workThesis}</p>
		<div class="chapter__works">
			{
				works.map((entry) => (
					<WorkCard work={entry.data} />
				))
			}
		</div>
	</section>
	<section id="about" class="chapter chapter--about">
		<div class="chapter__art" aria-hidden="true">
			<ChapterRule />
		</div>
		<p class="chapter__kicker">关于</p>
		<About />
		<Contact />
	</section>
	<Footer slot="footer" />
</BaseLayout>

<style>
	.chapter__works {
		width: min(var(--window-max), 100%);
		display: grid;
		gap: 3.5rem;
	}

	.chapter--about {
		scroll-margin-top: 6rem;
	}

	.chapter--about :global(.about),
	.chapter--about :global(.contact) {
		width: min(var(--window-max), 100%);
		padding: 1.25rem 0;
		text-align: left;
	}

	.chapter--about :global(.about h2),
	.chapter--about :global(.contact h2) {
		max-width: none;
		text-align: left;
	}
</style>
